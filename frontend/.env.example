# Copy this file to .env.local and fill in the values

# Backend API URL (leave blank to use vite dev proxy)
VITE_API_URL=http://localhost:8000

# API key — must match MEDGRAPH_API_KEY in root .env
VITE_API_KEY=dev-insecure-key-change-me
